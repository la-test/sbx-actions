---
version: 2
updates:
  # Keep npm dependencies up to date
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: "daily"
      time: "13:00"
    # Raise all npm pull requests with reviewers
    reviewers:
      - "btlogy"
